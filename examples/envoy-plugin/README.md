# Envoy Go Plugin Example

## Set up

From the root project folder build the plugin with:
```sh
make plugin
```

Then run with docker compse as:
```sh
docker compose -f ./examples/envoy-plugin/docker-compose.yaml up
```
